# 测试结果：T1 Supabase 数据层

**任务编号：** T1
**测试日期：** 2026-05-19
**测试方案：** [docs/test-plan/T1-supabase-data-layer.md](../test-plan/T1-supabase-data-layer.md)
**整体结论：** 🟡 部分通过（自动化 P0 用例 100% ✅ | TC-DB / TC-RLS 待 Supabase 在线验证）

---

## 执行汇总

| 分类 | 用例数 | 通过 | 失败 | 跳过 | 状态 |
|------|--------|------|------|------|------|
| TC-U 单元测试 | 39 | 39 | 0 | 0 | ✅ 全通过 |
| TC-S Seed 脚本 | 5 | 3 | 0 | 2 | ✅ 通过（含偏差说明） |
| TC-F 构建集成 | 4 | 3 | 0 | 1 | ✅ 通过 |
| TC-A 验收测试 | 4 | 3 | 0 | 1 | ✅ 通过（含待浏览器验证项） |
| TC-DB 数据库结构 | 5 | — | — | 5 | 🔲 待 Supabase SQL Editor |
| TC-RLS RLS 策略 | 4 | — | — | 4 | 🔲 待 Supabase SQL Editor |

---

## TC-U：单元测试（2026-05-19）

**执行命令：** `npx tsx scripts/test-unit.ts`
**结果：** 39 通过，0 失败

```
[TC-U1] isToolLevel — 合法枚举值
  ✅ TC-U1-a: "L1" 应返回 true
  ✅ TC-U1-b: "L2" 应返回 true
  ✅ TC-U1-c: "L3" 应返回 true
  ✅ TC-U1-d: "L4" 应返回 true

[TC-U2] isToolLevel — 非法值被拒
  ✅ TC-U2-a: "L5" 应返回 false
  ✅ TC-U2-b: "" 应返回 false
  ✅ TC-U2-c: null 应返回 false
  ✅ TC-U2-d: "l1"（小写）应返回 false
  ✅ TC-U2-e: "L0" 应返回 false

[TC-U3] isPricing — 合法枚举值
  ✅ TC-U3-a: "free" 应返回 true
  ✅ TC-U3-b: "freemium" 应返回 true
  ✅ TC-U3-c: "paid" 应返回 true

[TC-U4] isPricing — 非法值被拒
  ✅ TC-U4-a: "Free"（大写）应返回 false
  ✅ TC-U4-b: "subscription" 应返回 false
  ✅ TC-U4-c: null 应返回 false
  ✅ TC-U4-d: "" 应返回 false

[TC-U5] rowToTool — 完整行正确映射所有字段（12/12）
[TC-U6] rowToTool — null 字段降级（7/7，含 published_at 回退）
[TC-U7] rowToTool — 非法 level 降级为 L4（2/2）
[TC-U8] rowToTool — 非法 pricing 降级为 free（2/2）

单元测试结果：39 通过，0 失败
✅ 所有单元测试通过
```

---

## TC-S：Seed 脚本（2026-05-18）

**执行命令：** `npm run seed:tools`

| 用例 | 结果 | 说明 |
|------|------|------|
| TC-S1: seed 脚本执行成功 | ✅ | 输出 `✅ 成功写入 23 条`，退出码 0 |
| TC-S2: tools 表总数 | ⚠️ 23 条 | 规格写 24 条，实际 23 条（见偏差说明） |
| TC-S3: status=approved, source=curation | 🔲 | 需 Supabase SQL Editor 确认 |
| TC-S4: 必填字段无 NULL | 🔲 | 需 Supabase SQL Editor 确认 |
| TC-S5: 重复执行幂等 | ✅ | 二次执行无 duplicate key 错误 |

> **⚠️ 数据条数偏差：** `SEED_TOOLS` 数组共 23 条，规格文档描述为 24 条。经核查为 seed 数据本身即为 23 条，非写入丢失。需产品确认 23 条是否为最终有效数量。

---

## TC-F：构建集成（2026-05-18）

**执行命令：** `npm run build`

| 用例 | 结果 | 说明 |
|------|------|------|
| TC-F1: next build 无报错 | ✅ | `✓ Compiled successfully in 4.8s`，162 个静态页面，退出码 0 |
| TC-F2: 无 Supabase URL 时 build 报错 | 🔲 | 破坏性测试，未执行（需另行验证） |
| TC-F3: 产物 HTML 包含工具名 | ✅ | `tools/claude-code.html` 含 "Claude Code"（×2）和正确 tagline |
| TC-F4: 工具列表页包含多条工具 | ✅ | `tools.html` 含多个工具名，工具详情页 23 个 |

---

## TC-A：验收测试（2026-05-18）

| 用例 | 结果 | 说明 |
|------|------|------|
| TC-A1: data.ts 无硬编码，build 成功 | ✅ | `src/lib/data.ts:129` 为 `export const tools = await fetchToolsFromSupabase()`，build 成功 |
| TC-A2: 首页视觉一致 | 🔲 | 需 `npm run start` + 浏览器目视 |
| TC-A3: 工具详情页可渲染 | ✅ | 23 个 slug 页面均已生成，level/tagline 正确 |
| TC-A4: 分类过滤正确 | 🔲 | 需浏览器目视 |

---

## TC-DB：数据库结构（待 Supabase SQL Editor）

以下 SQL 需在 Supabase 控制台 → SQL Editor 中手动执行：

| 用例 | SQL | 期望 |
|------|-----|------|
| TC-DB1: tools 表列结构 | `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='tools' ORDER BY ordinal_position;` | slug/name 等必要列存在，is_nullable 正确 |
| TC-DB2: submissions 表列结构 | `SELECT column_name, data_type FROM information_schema.columns WHERE table_name='submissions';` | id/payload/source/status 等列存在 |
| TC-DB3: RLS 已启用 | `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename IN ('tools','submissions');` | 两行 rowsecurity=true |
| TC-DB4: 索引存在 | `SELECT indexname FROM pg_indexes WHERE tablename='tools';` | idx_tools_category / idx_tools_level / idx_tools_published_at |
| TC-DB5: updated_at 触发器 | `UPDATE public.tools SET name=name WHERE slug='claude-code';` 前后对比 updated_at | 触发器自动更新时间戳 |

---

## TC-RLS：RLS 策略（待 Supabase SQL Editor）

在 SQL Editor 中执行 `SET LOCAL ROLE anon;` 后测试：

| 用例 | SQL | 期望 |
|------|-----|------|
| TC-RLS1: anon 可读 approved | `SET LOCAL ROLE anon; SELECT count(*) FROM public.tools WHERE status='approved';` | 返回 23 |
| TC-RLS2: anon 无法读 draft | `SET LOCAL ROLE anon; SELECT count(*) FROM public.tools WHERE status='draft';` | 返回 0 |
| TC-RLS3: anon 无法读 submissions | `SET LOCAL ROLE anon; SELECT count(*) FROM public.submissions;` | 返回 0 或 permission denied |
| TC-RLS4: anon 可 INSERT submissions | `SET LOCAL ROLE anon; INSERT INTO public.submissions(payload,source) VALUES('{"n":"t"}'::jsonb,'anonymous_form');` | 插入成功 |

---

## 已知问题和偏差

| 编号 | 类型 | 描述 | 影响 | 建议 |
|------|------|------|------|------|
| D-1 | 数据偏差 | seed 数据 23 条，规格描述 24 条 | 工具列表页展示数量与规格不符 | 产品确认 23 条是否为有效数据量 |
| D-2 | 待验证 | TC-DB / TC-RLS 未执行 | 无法确认数据库结构和权限正确 | 在 Supabase SQL Editor 执行上方 SQL |
| D-3 | 待验证 | TC-A2 / TC-A4 浏览器验收未执行 | 无法确认视觉一致性和过滤功能 | `npm run start` 后人工目视 |

---

## 验收结论

```
✅ 可自动验证的 P0 用例（TC-U 39/39 + TC-S 3/3 + TC-F 3/4 + TC-A 2/4）全部通过
🔲 待完成：TC-DB1~5（Supabase SQL Editor）
🔲 待完成：TC-RLS1~4（Supabase SQL Editor）
🔲 待完成：TC-A2/A4（浏览器人工验证）
⚠️ 已知偏差：seed 数据 23 条 vs 规格 24 条，需产品确认

结论：代码层面验证通过，数据库层面验证依赖 Supabase 在线操作，
      完成 TC-DB / TC-RLS 确认后即可关闭 T1。
```
